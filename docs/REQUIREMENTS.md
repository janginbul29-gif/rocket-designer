# 요구사항 명세서 (REQUIREMENTS) — 로켓 설계기 (rocket-designer)

> 전체 문서 목차는 [INDEX.md](INDEX.md)를 참고하세요.
> 이 문서는 "우주여행 시간 지연 계산기 / 3D 발사 시뮬레이션" 프로젝트(별도 저장소, `goguggi/Space_project`)와
> 연동되는 **로켓 설계 프로그램**의 요구사항이다. 두 프로그램은 별도 저장소에서 개발하며,
> 연결 고리는 로켓 제원 JSON 파일(`.rocket.json`, 6절) 하나뿐이다.

## 1. 목적

KSP(Kerbal Space Program)처럼 3D 부품을 드래그로 조립해 다단 로켓을 설계하고,
조립 결과를 실시간으로 검증·계산(총질량, TWR, 단별/총 Δv)한 뒤 `.rocket.json`으로 내보내는 프로그램을 만든다.
내보낸 파일은 "우주여행 시간 지연 계산기 / 3D 발사 시뮬레이션" 프로젝트가 불러와 발사 장면을 재생하는 데 쓰인다.

## 2. 기능 요구사항

| 번호 | 기능 | 설명 |
|---|---|---|
| R-01 | 부품 목록 | 화면 한쪽에 부품 목록(팔레트)을 두고, 종류별로 묶어 보여준다 |
| R-02 | 3D 조립 | 부품을 장면으로 끌어다 놓으면 다른 부품의 연결점에 붙는다. 세로로 쌓기(직렬)와 옆에 붙이기(병렬 부스터)를 모두 지원한다 |
| R-03 | 단 편성 | 붙인 부품들을 "단"으로 묶고 분리 순서를 정한다. 병렬 부스터는 별도 단으로 표시된다 |
| R-04 | 회수 설정 | 단마다 "회수(재착륙)" 여부를 켜고 끈다. 착륙 다리 부품이 있는 단만 회수를 켤 수 있다 |
| R-05 | 실시간 계산 | 조립할 때마다 총질량, 발사 시 추력대중량비(TWR), 단별 Δv, 총 Δv를 계산해 표시한다 (3절) |
| R-06 | 검증 | 발사 가능 조건(4절)을 확인하고, 문제가 있으면 한국어로 이유를 보여준다 |
| R-07 | JSON 내보내기 | 조립 결과를 6절의 형식으로 저장한다 (다운로드) |
| R-08 | JSON 불러오기 | 저장한 파일을 다시 열어 조립 상태를 복원한다 |
| R-09 | 기준 로켓 | 팔콘 헤비와 같은 구성을 미리 만들어 둔 예제로 제공한다 ([examples/falcon_heavy.rocket.json](../examples/falcon_heavy.rocket.json)) |

## 3. 부품 종류 (최소 집합)

부품 하나는 아래 속성을 가진다. 부품 목록의 실제 값(카탈로그)은 `src/data/parts.js`에 정의하며,
단위와 속성 이름은 6절의 JSON 형식을 따른다.

| 종류 (`type`) | 필수 속성 | 비고 |
|---|---|---|
| `engine` | 건조 질량, 해수면 추력, 진공 추력, 해수면 Isp, 진공 Isp | 팔콘 헤비의 멀린 1D: 해수면 845 kN, 진공 914 kN, Isp 282/311 s, 약 470 kg |
| `tank` | 건조 질량, 추진제 질량 | 추진제 질량은 사용자가 채우는 양(0~최대)으로 조절 가능해야 한다 |
| `payload` | 질량 | 우주선(탑재체). 기본값 10 t |
| `fairing` | 질량 | 탑재체 덮개. 2단 점화 후 분리 |
| `decoupler` | 질량 | 단 경계. 이 부품 위아래가 다른 단이 된다 |
| `landing_legs` | 질량 | 붙어 있는 단만 회수 가능 |
| `booster_attach` | 질량 | 병렬 부스터를 옆에 붙이는 연결 부품 |

## 4. 계산해야 하는 값 (물리 근거)

아래는 학술적으로 정해진 공식이므로 그대로 쓴다.

| 값 | 공식 | 설명 |
|---|---|---|
| 총질량 m₀ | 모든 부품 건조 질량 + 추진제 질량의 합 | kg |
| 발사 시 추력대중량비 TWR | (첫 단에서 점화되는 엔진의 해수면 추력 합) / (m₀ · g₀) | 1보다 커야 이륙 |
| 단별 Δv | Isp_진공 · g₀ · ln(m_점화 시 / m_연소 종료 시) | 치올콥스키 로켓 방정식. m은 그 단 점화 시점에 로켓에 남아 있는 전체 질량 |
| 총 Δv | 단별 Δv의 합 | 병렬 부스터가 붙은 첫 단은 부스터 연소 구간과 코어 단독 구간을 나누어 계산 |
| 연소 시간 | 추진제 질량 / (추력 / (Isp · g₀)) | 초 |

상수: g₀ = 9.80665 m/s².

병렬 부스터가 있는 첫 단의 Δv는 두 구간으로 나눈다.

1. **부스터 연소 구간**: 코어(감쇠 추력 `throttleWhileBoosters`) + 부스터가 동시에 연소. 이 구간의 유효 배기속도는
   질량가중 평균 대신, 두 추진 계통의 합산 추력과 합산 질량유량으로 구한 유효 Isp를 사용한다:
   `Isp_eff = (F_core·throttle + F_boosters) / (mdot_core·throttle + mdot_boosters)` (m/s 단위, g₀로 나누면 초 단위),
   `Δv1 = Isp_eff · ln(m_전체_시작 / m_부스터_분리_직전)`.
2. **코어 단독 구간**: 부스터 분리 후 코어만 남아 연소를 계속하는 구간(있는 경우).
   `Δv2 = Isp_core_vac · g₀ · ln(m_부스터_분리_직후 / m_코어_연소_종료)`.

이 근사는 검증 예제(팔콘 헤비, 5절)에서 총 Δv가 상식적인 범위(지구 저궤도 도달 기준 약 9,000~13,000 m/s)에
드는지로 확인한다. 상세 유도가 필요하면 이 표를 갱신하고 [DECISIONS.md](DECISIONS.md)에 기록한다.

## 5. 발사 가능 조건 (검증 규칙)

내보내기 전에 아래를 확인한다. 실패하면 R-06에 따라 한국어 안내 문구를 보여주고, 치명적 실패(V-01~V-04, V-07)는
내보내기를 막는다. V-06은 경고만 하고 내보내기는 허용한다.

| 번호 | 조건 | 실패 시 안내 문구 (예) |
|---|---|---|
| V-01 | 탑재체가 정확히 1개 | "탑재체(우주선)를 하나 붙여 주세요" |
| V-02 | 단이 1개 이상, 각 단에 엔진 1개 이상과 탱크 1개 이상 | "N단에 엔진이 없습니다" |
| V-03 | 발사 시 TWR > 1 | "추력이 부족해 이륙할 수 없습니다 (TWR 0.8)" |
| V-04 | 회수를 켠 단에 착륙 다리가 있음 | "회수하려면 착륙 다리를 붙여 주세요" |
| V-05 | 회수를 켠 단의 남는 추진제 비율 ≥ 설정값 | 착륙 연소용 연료. 기본 5% |
| V-06 | 총 Δv ≥ 9,400 m/s | 지구 저궤도 도달에 필요한 대략값. 미달이면 경고만 |
| V-07 | 부스터 단은 반드시 병렬(옆에 붙음)이고, 직렬 단은 위아래로만 연결 | 구조 오류 |

## 6. 로켓 제원 JSON 형식 (두 프로그램의 계약)

파일 확장자는 `.rocket.json`, 인코딩은 UTF-8, 단위는 모두 SI 기본 단위(kg, N, s, m)다.
이 형식을 바꿔야 하면 반드시 양쪽 팀이 합의하고 `formatVersion`을 올린다.
완전한 예시는 [examples/falcon_heavy.rocket.json](../examples/falcon_heavy.rocket.json)에 있다.

```json
{
  "formatVersion": 1,
  "name": "팔콘 헤비 (예제)",
  "createdAt": "2026-09-05T00:00:00Z",
  "designer": "이름",
  "payloadMassKg": 10000,
  "stages": [
    {
      "id": "core",
      "label": "중앙 코어",
      "role": "serial",
      "separationOrder": 2,
      "dryMassKg": 22200,
      "propellantMassKg": 411000,
      "engines": { "count": 9, "thrustSeaLevelN": 845000, "thrustVacuumN": 914000, "ispSeaLevelS": 282, "ispVacuumS": 311 },
      "throttleWhileBoosters": 0.6,
      "recovery": { "enabled": true, "target": "drone_ship", "reservePropellantFraction": 0.05 }
    }
  ],
  "geometry": {
    "parts": [
      { "partId": "core-body", "type": "tank", "stageId": "core", "position": [0, 21, 0], "rotation": [0, 0, 0], "size": [3.7, 42, 3.7] }
    ]
  },
  "summary": { "totalMassKg": 1425000, "liftoffTwr": 1.6, "totalDeltaVMps": 12000 }
}
```

### 6.1 필드 설명

| 필드 | 필수 | 설명 |
|---|---|---|
| `formatVersion` | 예 | 형식 버전. 현재 1 |
| `name`, `createdAt`, `designer` | 예 | 표시용 |
| `payloadMassKg` | 예 | 탑재체(우주선) 질량. 기본 10,000 |
| `stages[]` | 예 | 단 목록 |
| `stages[].role` | 예 | `parallel`(옆에 붙은 부스터) 또는 `serial`(세로로 쌓인 단) |
| `stages[].separationOrder` | 예 | 분리 순서. 1부터. 병렬 부스터 2기는 같은 번호를 가질 수 있음 |
| `stages[].engines` | 예 | 그 단의 엔진 묶음. `count`개가 같은 제원. 추력과 Isp는 엔진 1개 값 |
| `stages[].throttleWhileBoosters` | 아니오 | 병렬 부스터가 붙어 있는 동안 코어 엔진 추력 비율(0~1). 없으면 1 |
| `stages[].recovery` | 예 | `enabled`, `target`(`launch_site` / `drone_ship`), `reservePropellantFraction`(착륙용으로 남길 비율) |
| `geometry.parts[]` | 예 | 3D 표시용. 위치, 회전, 크기(m). 발사 시뮬레이션 프로젝트는 이것으로 로켓 모양을 그린다. 물리에는 쓰지 않는다 |
| `summary` | 아니오 | 이 프로그램이 계산한 값. 불러오는 쪽은 참고만 하고 자체 계산으로 다시 검증한다 |

### 6.2 다른 프로젝트가 이 JSON을 읽는 방식 (참고)

"우주여행 시간 지연 계산기 / 3D 발사 시뮬레이션" 프로젝트는 발사 화면의 "설계한 로켓 불러오기" 버튼으로
`.rocket.json` 파일을 고른다. 파일을 5절과 동일한 규칙으로 검증하고, 통과하면 `stages`로 발사 물리를,
`geometry.parts`로 3D 형상을 만든다. (두 저장소가 같은 GitHub 계정의 Pages(같은 출처)에 있으면 파일 없이
브라우저 저장소로 바로 넘기는 방법도 가능하나, 채택 여부는 [DECISIONS.md](DECISIONS.md) Q-19에서 결정한다.)

## 7. 변경 이력

- 2026-09-05: 인수 문서 기준 최초 작성. 기존 Python 프로토타입(`Team project/rocket-designer/`,
  1차원 수직 상승 전용, 3D 조립·JSON 계약 없음)은 요구사항이 근본적으로 달라 별도 저장소로 새로 시작하고,
  기존 코드는 `Team project/_archive/rocket-designer-python/`에 보존했다.
