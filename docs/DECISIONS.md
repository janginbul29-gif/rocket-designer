# 미결 항목 및 의사결정 기록 (DECISIONS)

> 전체 문서 목차는 [INDEX.md](INDEX.md)를 참고하세요.
> 물리·학술적 사실이 아닌 임의의 설계 결정은 여기에 질문과 확정 결과를 기록한다.

## 양쪽 프로젝트가 함께 정해야 할 것 (인수 문서 9절, 미결)

| 번호 | 질문 | 제안 | 상태 |
|---|---|---|---|
| Q-19 | JSON 파일 외에 같은 출처 브라우저 저장소로 바로 넘기는 방식도 지원할지 | 파일 방식을 먼저 만들고, 나중에 추가 | 미결 — 파일 방식(R-07/R-08)으로 우선 구현 |
| Q-20 | 회수 단의 착륙용 예비 추진제 비율 기본값 | 5% | **확정: 5%** (REQUIREMENTS.md V-05, `reservePropellantFraction` 기본값) |
| Q-21 | 부품 목록의 실제 종류와 수치를 누가 정하는지 | 설계 담당자가 정하고, 팔콘 헤비 예제와 맞는지 다른 프로젝트에서 확인 | 이 저장소에서 `src/data/parts.js`로 확정. 팔콘 헤비 실측 공개값 기준 |
| Q-22 | Three.js 버전 | "시간 지연/발사 시뮬레이션" 프로젝트 도입 시 정한 버전을 공유 | 이 저장소는 우선 Three.js 0.160.0(2024년 초 릴리스, ES 모듈)을 사용. 상대 프로젝트와 버전 다르면 맞춘다 |
| Q-23 | 설계 프로그램 저장소 주소 | 만들어지면 기록 | 로컬 경로 `D:\rocket-designer` (git 저장소, 원격 미설정). 원격 저장소 주소 확정되면 이 항목 갱신 |

## 이 저장소 자체의 임의 결정

| 항목 | 결정 | 근거 |
|---|---|---|
| 병렬 부스터 구간 Δv 계산 방식 | REQUIREMENTS.md 4절의 "유효 Isp" 근사법 사용 | 인수 문서가 "03_physics.md 6.3절 질량 표를 따른다"고 했으나 해당 문서를 아직 전달받지 못해 표준적인 근사식으로 구현. 원본 문서 입수 시 재검토 |
| 코드 실행 확인 방법 | `npx serve` 또는 `python -m http.server` 등 정적 서버로 `index.html` 확인 | ES 모듈은 `file://`에서 CORS로 막히므로 |
| 테스트 실행 방법 | Node.js로 `node tests/*.test.js` 직접 실행 (별도 테스트 프레임워크 미사용) | 의존성 없이 바로 검증 가능하게 하기 위함 |

| 테스트 실행 환경 | Node.js가 로컬에 설치되어 있지 않아, `tests/*.test.js`는 아직 Node로 직접 실행해 확인하지 못했다. 대신 `tests/browser-check.html`을 정적 서버로 열어 동일한 로직(같은 `src/` 모듈)을 브라우저에서 검증했고 전체 통과했다 | 개발 환경 제약. Node 설치 후 `npm test`로 재확인 권장 |

## 변경 이력

- 2026-09-05: 인수 문서 수령 및 최초 작성
- 2026-09-05: A1~A2 구현, 브라우저 기반 테스트로 검증 (총질량 1,411.3 t / TWR 1.43 / 총Δv 13,651 m/s, 오류 없음). GitHub 저장소 `janginbul29-gif`로 초기 업로드
